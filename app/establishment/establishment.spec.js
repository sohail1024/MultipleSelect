'use strict';

describe('myApp.establishment module', function() {

  beforeEach(module('myApp.establishment'));

  describe('establishment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var establishmentCtrl = $controller('EstablishmentCtrl');
      expect(establishmentCtrl).toBeDefined();
    }));

  });
});