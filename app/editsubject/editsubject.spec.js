'use strict';

describe('myApp.editsubject module', function() {

  beforeEach(module('myApp.editsubject'));

  describe('editsubject controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editSubjectCtrl = $controller('EditSubjectCtrl');
      expect(editSubjectCtrl).toBeDefined();
    }));

  });
});