'use strict';

describe('myApp.editteacher module', function() {

  beforeEach(module('myApp.editteacher'));

  describe('editteacher controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editTeacherCtrl = $controller('EditTeacherCtrl');
      expect(editTeacherCtrl).toBeDefined();
    }));

  });
});