'use strict';

describe('myApp.manageOffice module', function() {

  beforeEach(module('myApp.manageOffice'));

  describe('manageOffice controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mangeOfficeCtrl = $controller('ManageOfficeCtrl');
      expect(mangeOfficeCtrl).toBeDefined();
    }));

  });
});