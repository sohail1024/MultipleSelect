'use strict';

describe('myApp.onlineexams module', function() {

  beforeEach(module('myApp.onlineexams'));

  describe('onlineexams controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var Onlineexams = $controller('OnlineExams');
      expect(Onlineexams).toBeDefined();
    }));

  });
});