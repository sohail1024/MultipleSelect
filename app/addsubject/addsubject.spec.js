'use strict';

describe('myApp.addsubject module', function() {

  beforeEach(module('myApp.addsubject'));

  describe('addsubject controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addSubjectCtrl = $controller('AddSubjectCtrl');
      expect(addSubjectCtrl).toBeDefined();
    }));

  });
});