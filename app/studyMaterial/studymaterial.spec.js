'use strict';

describe('myApp.studyMaterial module', function() {

  beforeEach(module('myApp.studyMaterial'));

  describe('studyMaterial controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var studyMaterialCtrl = $controller('StudyMaterialCtrl');
      expect(studyMaterialCtrl).toBeDefined();
    }));

  });
});