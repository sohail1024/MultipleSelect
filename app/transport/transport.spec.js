'use strict';

describe('myApp.transport module', function() {

  beforeEach(module('myApp.transport'));

  describe('transport controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var transportCtrl = $controller('TransportCtrl');
      expect(transportCtrl).toBeDefined();
    }));

  });
});